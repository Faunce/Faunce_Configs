i don't maintain this very well, so remember to update the vim plugins with :PlugUpdate

install tmux manually
install hyprland (if you want it)

run bootstrap.sh to bootstrap dotfiles
run bootstrap_hypr.sh to bootstrap hyprland configs


